{
  "name": "daily-dev",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist",
    "build:site": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "run": "node --env-file-if-exists=.env pipeline/run.ts",
    "check": "astro check"
  },
  "allowScripts": {
    "esbuild": true,
    "workerd": true,
    "fsevents": true
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.19",
    "@astrojs/sitemap": "^3.7.4",
    "astro": "^7.3.2"
  },
  "devDependencies": {
    "pagefind": "^1.5.2",
    "wrangler": "^4.132.0"
  }
}
